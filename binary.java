public class Binary
{
    int bsearch(int arr[], int item)
    {
        int lower=0;
        int upper = arr.length-1
        while(lower<upper)
        {
            int mid=(lower+upper)/2;
        }
        if(arr[m]==item)       //item found
        return m;
        if(arr[m]>item)
        {
            arr.length-1
        }
    }
}
